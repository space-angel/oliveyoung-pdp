# 비밀키
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# 재실행하면 다시 생기는 중간 산출물 (정본은 data/output/)
data/intermediate/*
!data/intermediate/.gitkeep
data/cache/*
!data/cache/.gitkeep

# macOS
.DS_Store

# 편집기
.vscode/settings.json
.idea/

# 공유 페이지 생성물 — 생성기(eval/share/*.py)와 템플릿만 커밋한다.
# 산출물이 바뀌면 다시 만들어야 하므로 커밋하면 조용히 낡는다 (실제로 v1 997건짜리가 남아 있었다).
eval/share/claims_page.html
eval/share/claims_browse.html
eval/share/claims_data.json
eval/share/deck_demo.json
