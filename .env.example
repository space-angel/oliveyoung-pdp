ANTHROPIC_API_KEY=sk-ant-...

# 작성자 해시 솔트 (PER-170). 임의의 32바이트 이상 문자열.
#   python3 -c "import secrets; print(secrets.token_hex(32))"
# 값이 바뀌면 스냅샷 간 authorHash가 달라져 중복 판정이 깨진다.
# 교체 시 AUTHOR_HASH_VERSION을 올리고 전체 재해시할 것.
AUTHOR_HASH_SALT=
AUTHOR_HASH_VERSION=v5-1
